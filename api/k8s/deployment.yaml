apiVersion: apps/v1
kind: Deployment
metadata:
  name: mindx-api
  namespace: dev
  labels:
    app: mindx-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mindx-api
  template:
    metadata:
      labels:
        app: mindx-api
    spec:
      imagePullSecrets:
        - name: acr-pull
      containers:
        - name: mindx-api
          image: mindxintern04acr.azurecr.io/mindx-api:v1.2.6
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: NODE_ENV
              value: "production"
            - name: FIREBASE_SERVICE_ACCOUNT_PATH
              value: "/app/secrets/firebase-service-account.json"
            - name: APPLICATIONINSIGHTS_CONNECTION_STRING
              value: "InstrumentationKey=98dd17f2-4b34-4619-9125-cbba8c125527;IngestionEndpoint=https://japaneast-1.in.applicationinsights.azure.com/;LiveEndpoint=https://japaneast.livediagnostics.monitor.azure.com/;ApplicationId=f7a0b1e6-eaac-4032-b404-db92646b0f22"
          volumeMounts:
            - name: firebase-secret
              mountPath: /app/secrets
              readOnly: true
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
      volumes:
        - name: firebase-secret
          secret:
            secretName: firebase-secret
